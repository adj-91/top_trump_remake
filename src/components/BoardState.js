import styled from 'styled-components';

const BoardState = ({gameState}) => {

    return (
        <div>
            <p>{gameState}</p>
            <p>BoardState</p>
        </div>
    )


    
}

export default BoardState;